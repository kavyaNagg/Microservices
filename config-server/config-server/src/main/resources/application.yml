server:
  port: 8083

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/kavyaNagg/microservice-configuration
          clone-on-start: true